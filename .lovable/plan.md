

## Plan: Add Second Logo Utility Page

Create a new isolated logo page at `/logo2` using the uploaded image, mirroring the existing `/logo` page pattern.

### Steps

1. **Copy uploaded image** to `src/assets/jadore-logo-full-gold.jpg`
2. **Create `src/pages/Logo2.tsx`** — same structure as `Logo.tsx`, centered logo on transparent background, no layout wrapper
3. **Add route** `/logo2` in `App.tsx`

