

## Plan: Change `/logo` route to `/logo.png`

Update the route path in `src/App.tsx` from `/logo` to `/logo.png` so the page is accessible at `https://jadore-wellness.com/logo.png`.

### Steps

1. **Edit `src/App.tsx`** — Change `<Route path="/logo" ...>` to `<Route path="/logo.png" ...>`

